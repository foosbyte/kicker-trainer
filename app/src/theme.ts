const defaultSpace = 14;

export const theme = {
  space: {
    l: defaultSpace * 2,
    m: defaultSpace,
    s: defaultSpace / 2,
    xl: defaultSpace * 4,
    xs: defaultSpace / 4,
    xxs: defaultSpace / 8,
  },
  color: {
    green: '#4FD49E',
    anthrazit: '#22292F',
    white: '#ffffff',
    red: '#FE2753',
    blue: '#094EFF',
    black: '#000000',
    grey: '#9CA1A5',
    transparentGrey: '#60676E4E',
  },
  font: {
    m: {
      fontSize: defaultSpace,
      lineHeight: defaultSpace * 1.14, // ~ 18px
    },
  },
  fontSize: {
    l: defaultSpace * 2,
    m: defaultSpace,
    s: defaultSpace / 2,
    xl: defaultSpace * 4,
    xs: defaultSpace / 4,
    xxs: defaultSpace / 8,
  },
};
